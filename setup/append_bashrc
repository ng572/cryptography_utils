# appneded by setuptools
alias unlock="mkdir -p ./mytmpfs && sudo mount -t tmpfs -o size=10M tmpfs ./mytmpfs && openssl bf -d -a -in crypto_squared -out ./mytmpfs/code && python3 ./mytmpfs/code ; sudo umount ./mytmpfs && rm -r ./mytmpfs"
